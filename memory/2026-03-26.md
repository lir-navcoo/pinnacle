# Daily Log - 2026-03-26

- 完成Pinnacle（里程碑）战报配置平台的架构设计和技术选型
- 确定前端可视化编辑器技术方案：Fabric.js + React + shadcn/ui
- 确定后端技术栈：FastAPI (Python) 全栈实现
- 完成项目初始化，前后端基础框架已搭建完毕
- 后端：FastAPI + SQLAlchemy + Prisma schema + 核心服务（渲染、钉钉、OSS）
- 前端：Next.js 15 + shadcn/ui + Fabric.js 可视化编辑器组件
- 已完成本地 Git 提交，待推送到 Gitee 远程仓库
- 已将代码推送到 Gitee 远程仓库（https://gitee.com/li78080114/pinnacle.git）
- 完成项目可用性验证：后端 FastAPI 成功加载 29 个路由，前端 Next.js 结构完整
- 修复关键依赖问题：添加 pydantic-settings、greenlet、oss2 包
- 确认运行前提：需要 PostgreSQL 数据库、可选 Redis 和阿里云 OSS
- 完成基础设施配置切换：数据库从 PostgreSQL 改为 MySQL（使用 aiomysql），对象存储从阿里云 OSS 改为本地文件存储
- 创建本地存储服务 storage_service.py，统一存储接口支持本地/OSS 切换
- 更新环境变量配置模板 env.example 和数据库初始化脚本 init_db.sql
- 项目已适配用户本地环境：MySQL + Redis + 本地文件存储，可直接启动验证
- 完成基础设施配置切换：数据库从 PostgreSQL 改为 MySQL（使用 aiomysql），对象存储从阿里云 OSS 改为本地文件存储
- 创建本地存储服务 storage_service.py，统一存储接口支持本地/OSS 切换
- 更新环境变量配置模板 env.example 和数据库初始化脚本 init_db.sql
- 项目已适配用户本地环境：MySQL + Redis + 本地文件存储，可直接启动验证
- 用户确认本地已有 MySQL 和 Redis 服务，OSS 使用本地文件存储逻辑替代
- 用户确认本地已有 MySQL 和 Redis 服务，OSS 使用本地文件存储逻辑替代
- 完成基础设施配置切换：数据库从 PostgreSQL 改为 MySQL（使用 aiomysql），对象存储从阿里云 OSS 改为本地文件存储
- 创建本地存储服务 storage_service.py，统一存储接口支持本地/OSS 切换
- 更新环境变量配置模板 env.example 和数据库初始化脚本 init_db.sql
- 项目已适配用户本地环境：MySQL + Redis + 本地文件存储，可直接启动验证
- 尝试启动前后端服务时遇到系统文件描述符限制问题（ulimit -n），需用户手动释放资源后启动
- 已通过定时任务安排自动启动前后端服务（后端端口 8000，前端端口 3000）
- 前端服务已成功启动（端口 3000），后端服务因依赖问题需手动启动（端口 8000）
- 已安装缺失的 pydantic-settings 依赖，后端可正常启动
- 前后端服务已成功启动并验证可用：前端运行在 3001 端口，后端运行在 8000 端口
- 解决启动问题：前端因 3000 端口被占用自动切换到 3001 端口，后端安装 pydantic-settings 依赖后正常启动
